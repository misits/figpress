import * as React from "react";

function Footer() {
  return (
    <footer className="app-footer">
      <p>FigPress &copy; {new Date().getFullYear()}</p>
    </footer>
  );
}

export default Footer;
